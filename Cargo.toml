[package]
name = "lava"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0.95"
clap = { version = "4.5.23", features = ["derive"] }
mlua = { version = "0.10.2", features = ["error-send", "luau-jit"] }
